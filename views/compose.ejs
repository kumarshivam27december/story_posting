<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Compose</title>
</head>
<body>
  <h1>Compose a New Story</h1>
  <form action="/compose" method="POST">
    <input type="text" name="postTitle" placeholder="Story Title" required>
    <textarea name="postContent" placeholder="Story Content" required></textarea>
    <button type="submit">Submit</button>
  </form>
  
  <a href="/">Back to Home</a>
  
  <% if (user) { %>
    <a href="/logout">Logout</a>
  <% } else { %>
    <a href="/login">Login</a>
    <a href="/register">Register</a>
  <% } %>
</body>
</html>
